hi
<h3>
Your futur events
</h3>
<div >
<% @events.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.description %></td>
    <td><%= event.category%></td>
    <td><%= event.start_at %></td>
    <td><%= event.end_at %></td>
  </tr>
  <% end %>
</div>

<h3>
event jou joined
</h3>
.
.
.

<h3>
your categories
</h3>

<% @favourites.each do |fav| %>
  <p>
    Favorite Sport: <%= fav.category.sport %>
  </p>
<% end%>

<h4> ADD </h4>
<div>
  <% Category.all.each do |cat| %>
    <div>
      <p><%= cat.sport %></p>
      <%= link_to "Fav", category_favourites_path(cat), data: { turbo_method: :post } %>
      <%= link_to "delete", category_favourite_path(cat), data: { turbo_method: :destroy } %>

    </div>
  <% end %>
</div>
